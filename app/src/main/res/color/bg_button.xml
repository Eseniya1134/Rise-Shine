<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@drawable/bg_button" android:color="@color/bgMenu" />

    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@drawable/bg_button" android:color="@color/bgMenu" />

    <!-- Default state -->
    <item android:drawable="@drawable/bg_button" android:color="@color/bgMenu" />
</selector>